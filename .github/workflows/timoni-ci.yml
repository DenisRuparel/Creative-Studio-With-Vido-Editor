name: Creative Studio CI

on:
  push:
    branches: master

env:
  REGISTRY: 13.233.132.176
  PROJECT: creative-studio
  IMAGE_NAME: creative-studio
  # PLUGIN_IMAGE_NAME: timoni-cmp
  
permissions:
  contents: write

jobs:
  # build-plugin:
  #   name: Build Timoni CMP image (if changed)
  #   runs-on: self-hosted
  #   outputs:
  #     plugin_changed: ${{ steps.plugin-check.outputs.changed }}
  #     plugin_digest: ${{ steps.plugin-digest.outputs.digest }}

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 2

  #     - name: Detect plugin changes
  #       id: plugin-check
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         if git diff --name-only "${{ github.event.before }}" "${{ github.sha }}" | grep -q '^argocd/cmp/timoni/'; then
  #           echo "changed=true" >> "$GITHUB_OUTPUT"
  #           echo "Timoni CMP changed"
  #         else
  #           echo "changed=false" >> "$GITHUB_OUTPUT"
  #           echo "Timoni CMP unchanged"
  #         fi

  #     - name: Login to Harbor
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         echo "${{ secrets.HARBOR_PASSWORD }}" | docker login "${REGISTRY}" \
  #           -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin

  #     - name: Build plugin image
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         docker build \
  #           -t "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:${GITHUB_SHA}" \
  #           -t "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:latest" \
  #           -f argocd/cmp/timoni/Dockerfile \
  #           argocd/cmp/timoni

  #     - name: Push plugin image
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         docker push "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:${GITHUB_SHA}"
  #         docker push "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:latest"

  #     - name: Get plugin image digest
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       id: plugin-digest
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         # Ensure RepoDigests is populated from registry
  #         docker pull "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:${GITHUB_SHA}" >/dev/null
  #         DIGEST_REF="$(docker image inspect "${REGISTRY}/${PROJECT}/${PLUGIN_IMAGE_NAME}:${GITHUB_SHA}" --format '{{index .RepoDigests 0}}')"
  #         echo "digest=${DIGEST_REF}" >> "$GITHUB_OUTPUT"
  #         echo "Plugin digest: ${DIGEST_REF}"

  #     - name: Update repo-server sidecar patch to latest digest
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         PATCH_FILE="argocd/patches/argocd-repo-server-timoni-cmp.yaml"
  #         sed -i "s|^\\(\\s*image:\\s*\\).*${PROJECT}/${PLUGIN_IMAGE_NAME}.*$|\\1${{ steps.plugin-digest.outputs.digest }}|" "${PATCH_FILE}"

  #     - name: Commit patch update (if any)
  #       if: steps.plugin-check.outputs.changed == 'true'
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         if git diff --quiet; then
  #           echo "No patch changes to commit."
  #           exit 0
  #         fi
  #         git config user.name "github-actions"
  #         git config user.email "actions@github.com"
  #         git add argocd/patches/argocd-repo-server-timoni-cmp.yaml
  #         git commit -m "chore(argocd): bump timoni cmp image digest"
  #         git push origin "${GITHUB_REF_NAME}"

  build-app:
    name: Build application image and trigger CD
    runs-on: self-hosted
    # needs: build-plugin

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Harbor
        shell: bash
        run: |
          set -euo pipefail
          echo "${{ secrets.HARBOR_PASSWORD }}" | docker login "${REGISTRY}" \
            -u "${{ secrets.HARBOR_USERNAME }}" --password-stdin

      - name: Build app image
        shell: bash
        run: |
          set -euo pipefail
          docker build \
            -t "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:${GITHUB_SHA}" \
            -t "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:latest" \
            .

      - name: Push app image
        shell: bash
        run: |
          set -euo pipefail
          docker push "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:${GITHUB_SHA}"
          docker push "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:latest"

      - name: Get app image digest
        id: image-digest
        shell: bash
        run: |
          set -euo pipefail
          docker pull "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:${GITHUB_SHA}" >/dev/null
          DIGEST_REF="$(docker image inspect "${REGISTRY}/${PROJECT}/${IMAGE_NAME}:${GITHUB_SHA}" --format '{{index .RepoDigests 0}}')"
          echo "image_digest=${DIGEST_REF}" >> "$GITHUB_OUTPUT"
          echo "App digest: ${DIGEST_REF}"

      - name: Trigger CD pipeline (repository_dispatch)
        shell: bash
        run: |
          set -euo pipefail
          curl -fsSL -X POST \
            "https://api.github.com/repos/DenisRuparel/Timoni-implementation-with-ArgoCD-Plugin---DevOps/dispatches" \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.REPO_TOKEN }}" \
            -d '{
              "event_type": "update-image",
              "client_payload": {
                "image_digest": "'"${{ steps.image-digest.outputs.image_digest }}"'"
              }
            }'
